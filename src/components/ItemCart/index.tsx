import React from "react";
import * as styles from './styles'

interface cart {
    color: string;
    numbers: number[];
    data: string;
    price: number;
}

export const ItemCart = () => {
    return (
        <styles.Container>
            
        </styles.Container>
    )
}